# Test cmd:
python -m pytest tests/[file name]

# Freeze requirements
pip freeze > requirements.txt

# Docker compose action
docker-compose up --build
docker-compose up -d redis-db
docker-compose down